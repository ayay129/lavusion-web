# 览悟找图 · 本地图库同步

## Style Prompt
An eight-second silent homepage animation showing the real local-gallery concept: several watched folders remain visibly synced, while one new image is indexed into the selected `手袋印花` folder. Use a handful of real transparent artwork files from the supplied folder. Keep the animation calm and legible inside the homepage's pale feature panel.

## Colors
- Canvas #f5f5f7, white surfaces, fine #e6e6e9 borders.
- Ink #17171a and secondary #707076.
- Green sync dots #25a66a, matching the source UI's synced state.
- Selected-folder treatment uses a quiet pale-blue outline.

## Motion
Reveal the source-folder list and the `手袋印花` gallery with eight existing images. Add a ninth image: enlarge it in the open third row, show automatic indexing and a count of 246, then move it into the first cell of that row. Turn only the selected folder's sync dot gray while indexing, then return it to green when the index is ready. Preserve all eight existing images and hold the final nine-image composition; do not loop.

## What NOT to Do
- All five supplied folders start green, matching the screenshot. A temporary gray dot is used only on `手袋印花` while the demonstrated file change is being indexed.
- Do not invent filenames, search results, cloud uploads, or software chrome.
- Keep all eight original images and the newly added ninth image visible in the final gallery.

## Source provenance
The source UI screenshot shows the selected flower folder at 245 images; the homepage animation increments it to 246. At the user's request, the other displayed folder counts are enlarged from the screenshot values: 演示1绣花26.7.18 (910), 演示2印花26.6.26 (1,240), 演示3印花26.7.7 (1,380), and 演示4印花26.6.14 (610). The selected `flower` folder is displayed as `手袋印花`. Thumbnail assets are reduced transparent PNG previews copied from `WY000004411416.png` through `WY000004411424.png` in the user's `/Volumes/ORICO/布料/flower` folder; source files are not modified.
